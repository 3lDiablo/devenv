apiVersion: v1
kind: Service
metadata:
  name: kube-state-metrics
  namespace: infrastructure
  labels:
    component: kube-state-metrics
spec:
  ports:
  - port: 8080
    targetPort: 8080
    name: http-metrics
  selector:
    app: kube-state-metrics
