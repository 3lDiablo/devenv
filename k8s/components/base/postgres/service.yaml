apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: infrastructure
  labels:
    component: postgres
spec:
  type: NodePort
  selector:
    app: postgres
  ports:
    - name: tcp-postgres
      port: 5432
      targetPort: 5432
      nodePort: 30432
