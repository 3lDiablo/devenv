apiVersion: kafka.strimzi.io/v1
kind: KafkaTopic
metadata:
  name: local-card-tokenisation-event-v1
  namespace: infrastructure
  labels:
    strimzi.io/cluster: kafka-cluster
spec:
  partitions: 2
  replicas: 1
  config:
    cleanup.policy: delete
    retention.ms: "-1"
